# MEMORY.md
Durable, append-only project notes (newest on top). **Keep token-efficient: dense, minimal blank lines, lines may exceed 80 chars.**

## Project facts
- **What:** personal finance tracker. **Since v2.0.0 (2026-08-23) the backend is a Cloudflare Worker over Cloudflare D1.** Google Sheets is a frozen read-only archive, not the source of truth.
- **Cloudflare Worker:** `financetracker-telegram` (free plan) — serves the PWA, `/api` (D1), the Telegram bot at `/tg`, and one cron job (IBKR prices). **Never rename it:** `name` is the `.workers.dev` subdomain, i.e. both the app URL and the webhook target. Deployed from `worker/` with `npx wrangler deploy` (also run by `npm run release`).
- **D1 database:** `financetracker`, region `apac`. Schema in `worker/migrations/`. Money is **integer micros**; month keys are `yyyy-MMM`; balances are computed at read, never stored.
- **Apps Script ID:** `1IiUkd6dyJFnLuGXjjcS_CEohyeZLsXNO3eqFGm438IQL02dyhUxjgarE` (linked via `.clasp.json`). **No Web App deployment any more** — two trigger-driven files only (`Gmail.gs` courier, `Backup.gs` nightly dump). `GmailApp` is the sole reason GAS survives.
- **Frontend:** the PWA in `worker/public/` — 7 screens (Dashboard · Transactions · Budgets · Accounts · Swap · Tax · Admin), offline-capable since v1.6.1. The `/api` JSON contract did not change at the D1 cutover, so `app.js` was untouched apart from the Admin screen.
- **Locale:** PHP; timezone `Asia/Manila`. Every "today" goes through `manilaToday()` — Workers and D1 run in UTC.
- **Share prices:** IBKR Flex Web Service, Cloudflare cron 06:00 Manila, into the `prices` table. GOOGLEFINANCE is gone with the Sheet. **Nothing fetches a price on the read path.**
- **Status:** v1 (GAS + Sheets) ran v1.0.0→v1.9.2. `git show v1.9.2:CLAUDE.md` for the sheet-era lore; `git show v2.0.0:D1_MIGRATION.md` for the cutover plan.

## Deployment & triggers
- **Everything live is the Worker.** `npm run release` = D1 migrations → `wrangler deploy` → tag → push → GitHub Release. A UI-only change still needs the deploy; a GAS change needs `npm run push` and nothing else (no deployment exists to redeploy).
- **Worker secrets** (Cloudflare only): `APP_PASS`, `SECRET_TOKEN`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_USER_ID`, `GEMINI_API_KEY`, `INGEST_TOKEN`, `IBKR_FLEX_TOKEN`, `IBKR_FLEX_QUERY_ID`. `worker/.dev.vars` (gitignored) holds the same names for `wrangler dev`. **`GAS_URL` is deleted.**
- **GAS script properties:** `WORKER_URL`, `INGEST_TOKEN`, optional `GMAIL_HINTS`/`GMAIL_QUERY` (neither is set — `cfg_` falls back to the `GMAIL_HINTS_`/`GMAIL_QUERY_` constants in `Gmail.gs`), plus the machine-written `GMAIL_LAST_TS` and `BACKUP_SHEET_ID`. Everything else was deleted or moved.
- **Settings that used to be script properties now live in the `meta` table** and are editable on the Admin screen: `monthly_income_php`, `usd_php_fallback`, `owner_email`, `ledger_first_year` (+ machine-written `data_version`, `tg_last_ids`).
- **Triggers:** one Cloudflare cron, `0 22 * * *` (IBKR prices) — in `wrangler.toml`, so it IS in source now. Apps Script keeps two: `gmail_ingest` every 5 min, and `backup_run` nightly (install with `backup_install()`). **Cloudflare does not retry a failed cron** — `runCron` catches and Telegrams the owner.
- **Backups, three layers:** the nightly Apps Script pull into a spreadsheet, the Admin screen's per-table CSV, and D1 Time Travel (7 days, free).
- **Free-tier headroom:** static assets are unmetered; only `/api`, `/login`, `/tg` and the crons burn invocations — a few hundred a day against 100k. D1 usage is thousands of read rows and dozens of writes against 5M/100k. The 10ms CPU cap excludes I/O wait, and the dashboard's aggregation is pushed into SQL for exactly that reason.

## Database tables (keep in sync with code)
`account_types · accounts · categories · transactions (+2 generated columns) · budgets · recurring · ledger (+ ledger_view) · prices · nw_snapshots · email_quotes · seen_updates · meta`. Full schema and the reasoning: `worker/migrations/0001_init.sql` and the **`worker-backend` skill**. The old sheet column lists are in `git show v1.9.2:MEMORY.md`.

## Decisions
- 2026-09-04: **`npm run push` had been broken since 2026-08-24, and the repair of it then knocked out every Apps Script trigger.** Two findings, one chain. ① `.claspignore` allowed `!**/*.js` and then removed the local tools ONE BY ONE BY NAME (`release.js`, `test.js`, `test-api.js`, `icons.js`); `dev-pull.js` (2026-08-24), `bootstrap.js` (2026-08-29) and `stamp-version.js` (2026-08-30) each arrived after that list was written, clasp compiles every pushed `.js` as a `.gs`, and a `#!/usr/bin/env node` on line 1 fails the WHOLE push. So the Apps Script half ran 11-day-old code and nobody knew — `npm run push` is the only path to GAS and nothing else exercises it. **Fix: the `.js` allow line is gone entirely.** Apps Script runs no `.js` in this project, so a new tool file now needs no edit there; naming files one by one was the bug, not the list being short. `npx clasp status` is the check — it must print exactly `appsscript.json`, `Backup.gs`, `Gmail.gs`, `Tests.gs`. ② The first successful push in 11 days carried the 2026-08-26 deletion of **`Export.gs`**, whose `DriveApp.createFile` was the only caller of the Drive scope. `appsscript.json` declares NO `oauthScopes`, so Apps Script recalculates the scope set on every save — the set SHRANK, which stales the stored grant exactly as a growing set does, and every trigger began failing with "Authorization is required to perform that action." **Repair is one action:** run a function from the editor and accept the consent screen. **Expect this after any push that adds or removes a `.gs`.** Pinning `oauthScopes` would stop it recurring and was NOT done: guessing the list wrong breaks the mail courier silently, and a `.gs` file changes about twice a year here. Read the real list off the editor's Overview page first if it is ever worth doing.
- 2026-09-04 (v2.11.2 — the Gmail courier lost three transactions): **a post that THREW no longer moves the watermark past its message.** Symptom the owner saw: two MariBank alerts from 2026-09-03 (DQ Cherry ₱56.43, MercuryDrug ₱66.75) and one from 2026-09-04 (a ₱1000 BPI BanKo → MariBank transfer) were still in the inbox and had no rows, while alerts an hour either side of them landed normally. **Proof of the mechanism, not a guess:** `email_quotes` had no row for any of the three, and `ingestEmail` writes the quote BEFORE `logItems` — so the parse never returned, i.e. the POST threw. The old `gmail_ingest` did `newest = max(newest, ts)` before the try and wrote it unconditionally, so one transient Gemini failure retired the message for ever; the mail stayed in the inbox with nothing looking at it again. **The comment that justified this was wrong:** it feared an unparseable email replaying every 5 minutes, but a genuine non-transaction comes back HTTP 200 `logged 0 of 0`, which is a SUCCESS on this path — only a real fault (Gemini 429/5xx/timeout, Worker down, bad token) throws. **The fix is 4 lines:** `hold = min(hold, ts - 1)` in the catch, and the watermark is `min(newest, hold)` — so the mark stops at the OLDEST failure in a run even when a later message succeeded in the same run. `GMAIL_RETRY_MS_` (3h ≈ 36 tries) bounds it, because a permanently poisonous email would otherwise spend a Gemini call every 5 minutes against a free tier of ~250 a day. Three vm-driven tests in `test.js` hold all three cases.
- 2026-09-04: **Cloudflare Workers Logs is now ON (`[observability]` in `wrangler.toml`).** It is OFF by default, which is why the three failures above left nothing to read: `worker.js` has written `console.error(action + ...)` for every throwing handler since v2.0.0, but nobody was keeping the output — `wrangler tail` shows only the present. Inherited by `[env.staging]`, free plan, `head_sampling_rate = 1` because this Worker does tens of requests a day. **The Apps Script half cannot be done from a session:** the Executions page's "Cloud logs" tab stays empty until a person attaches a standard GCP project in Project Settings, and a 0s "Failed" execution (one happened 2026-09-04 19:09) fails before any user code, so nothing would have been logged there either — the trigger's own "Notify me immediately" failure email is the setting that actually catches those.
- 2026-09-02 (v2.12.0, option ② — the floor under the silent-bot repair): **the update-id claim became a QUEUE, and a 2-minute cron re-runs the turns that never finished.** v2.11.0 removed Cloudflare's `waitUntil` cancellation by awaiting the turn, and in doing so moved the ceiling onto **Telegram's patience for a webhook reply — a number Telegram does not document**. A turn cut off there dies the same way the old one did: nothing throws, so nothing above reports it, and `seen()` has already claimed the update, so the redelivery is dropped as a duplicate. The message is gone in silence again. **The root ambiguity is the claim itself:** "someone took this" and "this was lost" were the same row. `seen_updates` now stores the raw update (`payload`), whether the turn ended (`done`) and how many rescues it has had (`attempts`), so an unfinished row IS recoverable work; `drainUpdates()` re-runs it. **Six decisions worth keeping.** ① The drain calls `route()`, never `handleUpdate()` — `handleUpdate` claims first and would see its own claim and rescue nothing. ② `STALE_MS` (120s) sits far above `TURN_CEILING_MS` (25s): being early costs a second receipt for a message the owner is still waiting on, which is the confusion this whole area exists to remove; nothing on Cloudflare survives two minutes of wall clock, so a row that old is not running. ③ `at` is re-stamped BEFORE the work, which both stops two drains racing the same row and gives a repeat failure its back-off. ④ `MAX_ATTEMPTS` (3) then one ❌ message — a payload that can never succeed would otherwise fail 720 times a day and say nothing, which is the old bug with a clock attached. ⑤ The sweep is scoped to `done = 1`; deleting a pending row is the silent loss, restored. ⑥ `settle()` clears the payload — the retry window is the only reason to hold the owner's message text. **Both entry points now AWAIT:** `scheduled` was still handing `runCron` to `ctx.waitUntil`, which is the same trap on the other door, and `event.cron` picks the job because sending the daily IBKR pull to the 2-minute clock would trip its ten-per-minute ceiling and kill the prices job outright. A contract guard holds `wrangler.toml`'s cron list equal to the constants in `jobs.js` — a drifted string is silent in BOTH directions (no drain, and the daily job falls through to a warning). **Safety comes from the idempotent row ids, not from locking:** a rescue that races a turn which was merely slow writes nothing twice, it just re-sends the receipt as "Already logged", and `test-api.js` proves that by abandoning a real turn mid-parse, draining it, then letting the abandoned turn resume. **Every guard was proved to fail against the old shape before it was trusted.** **Cost:** ~720 extra invocations a day against a 100k/day free ceiling, almost all of them one indexed read that finds nothing. **Standing lesson:** a dedup claim that does not say whether the work FINISHED is a way to lose work, not a way to protect it.
- 2026-09-02 (v2.11.0, the second half of the silent-bot repair): **`/tg` AWAITS the turn — the work moved out of `waitUntil` and into the request.** v2.10.2 bounded the parse so a slow model became a REPORTED failure instead of a silent one. It did not save the message, and the owner asked the right follow-up: can we say "still working" and keep waiting? **Not inside a `waitUntil` task** — a cancelled one is torn down, so there is nothing left to send a notice WITH and nothing left to wait with. The fix is to stop deferring: `/tg` now runs `await handleUpdate(...)` and answers Telegram afterwards. **The justification is the bug's own evidence:** `ingestEmail` never suffered this, and the only structural difference was that Apps Script holds its connection open — a PENDING REQUEST is not governed by the waitUntil allowance. So the shape that was already provably surviving is the shape the chat path now uses. The old comment claimed a parse is "not inside Telegram's patience"; a healthy parse is a few seconds, and the whole turn is now bounded under `TURN_CEILING_MS`. **`whileSlow()` is the piece the owner asked for:** if the parse passes `SLOW_NOTICE_MS` (6s) it sends one "⏳ Still working…" and KEEPS waiting; the notice is deliberately fire-and-forget, because awaiting a courtesy would let a failed courtesy sink the receipt behind it, and the timer is always cleared so a fast turn sends nothing extra. **The budget grew because the room grew** (`PARSE_BUDGET_MS` 12s → 20s, `MODEL_TIMEOUT_MS` 7s → 9s) — but a room is still a room, and a guard asserts `PARSE_BUDGET_MS + 4s <= TURN_CEILING_MS` plus `SLOW_NOTICE_MS < PARSE_BUDGET_MS`, since a notice that fires after the parse gives up is not a notice. **The guard that matters most is the source-text one:** `/tg` must contain `await handleUpdate(` and must NOT contain `waitUntil` — handing the turn back to `waitUntil` reads as a harmless latency win and silently restores the original loss. It strips comments first, because that handler now EXPLAINS the trap at length and a prose-reading guard fails on its own documentation. Proved to fail against the old shape before it was trusted. **Honest limit:** the ceiling MOVED, it did not vanish — see the TODO. **Cost accepted:** `/tg` now holds a connection for the length of a turn instead of answering in milliseconds. For a one-user bot on a 100k/day ceiling that is free; it would matter at fan-out, and that is when option ② earns its complexity.
- 2026-09-02: **a Telegram turn ends in a message or in a released claim — never in silence.** Owner sent "749 maribank Mobile data 60gb" and got NOTHING back: no receipt, no error, no row. The diagnosis that matters is what the silence ruled out — the Gmail courier was still delivering receipts that same hour, and it shares `parse`, `logItems`, `createTransaction` and `send` with the chat path, so Gemini, D1 and the outbound bot were all provably healthy. The fault could only be in the inbound-only segment: the webhook delivery, `seen()`, `refs()`, or the Worker dying inside `waitUntil`. **The bot could not tell the owner which**, and that was the real defect. `route()` answers its own errors, but everything AROUND it reached `console.error` only, and a Workers log nobody tails is the same as nothing. **Worse, the failure was permanent.** `seen()` claimed the `update_id` BEFORE the work, so Telegram's own redelivery — the platform's built-in second chance — was dropped as a duplicate. One bad minute lost the message for good, and the owner had no way to distinguish a bot that refused the message from a webhook that never delivered it. **The fix is two halves that meet.** ① `handleUpdate` REPORTS an escaping error into the chat it came from. ② If it cannot even report, it calls `unsee()` to RELEASE the claim, so the redelivery gets a real attempt. Releasing is safe only because the row ids are already idempotent (`tg-<update_id>-<i>` + `ON CONFLICT DO NOTHING`), so a retry can add the missing receipt but never a second row — a test asserts exactly that, and it comes back "Already logged" because the row lands BEFORE the send fails. **The claim is KEPT whenever the failure was reported**, which is what stops the retry storm the claim exists for: a redelivery must not repeat the same complaint. `call()` now returns whether Telegram accepted the message, and still THROWS on a network fault — that throw is load-bearing, because it is what makes the Gmail courier hold the mail and re-ingest instead of trashing a receipt nobody saw. **`handleUpdate` had ZERO test coverage before this**, which is why a whole class of silent failure lived in the one code path with no other feedback channel; `test-api.js` now drives four real turns (healthy + redelivery, a broken `refs()` read, an unreportable failure, and the released retry) against the live SQLite. **ROOT CAUSE FOUND, and it is NOT what the reporting fix covers.** `getWebhookInfo` came back with the right URL, both allowed update types, `pending_update_count: 0` and **no `last_error_*` fields at all** — so Telegram delivered every message and the Worker answered 200 each time. `wrangler tail` then named it: `waitUntil() tasks did not complete within the allowed time after invocation end and have been cancelled`. **A cancelled waitUntil task is torn down, not rejected** — it throws nothing, so `handleUpdate`'s catch never runs, `report()` never fires and `unsee()` never fires. The tail proves it: the runtime warning appeared with NO `telegram:` line beside it. That also explains the `seen_updates` row with no transaction — `seen()` finished, the parse did not. And Telegram had its 200, so no redelivery was ever coming for the released claim to catch. **The real fault is an unbounded parse.** `tryModels` walked three models with no per-attempt timeout and no overall budget; measured against the old code, three failing attempts spend ~21s, and `MODELS` are all `-latest` ALIASES, so Google can repoint one at a slower model and a parse that took two seconds last month takes thirty today with nothing changed here. `ingestEmail` is unaffected because it runs inside a PENDING request, which is not what waitUntil's allowance governs — that asymmetry is exactly why the email path kept working and is the thing that made the bug look impossible. **The fix is a self-imposed clock:** `AbortSignal.timeout` per attempt (`MODEL_TIMEOUT_MS` 7s), a budget across the whole chain (`PARSE_BUDGET_MS` 12s) that stops the fallback when the time is gone, and the abort translated into a sentence rather than a leaked DOMException. **The slack is the feature** — the budget sits well inside the allowance precisely so the "Gemini did not answer" message has time to send; a contract guard fails the build if `PARSE_BUDGET_MS` creeps past 20s. A second guard drives the chain with an INJECTED clock (no 12 real seconds in the suite) and was proved to fail against the old implementation. **Keep both halves:** the reporting fix does not cover a cancellation and the budget does not cover a thrown error, so neither is redundant. **Standing lesson:** in `waitUntil`, an unbounded await is not a slow turn, it is a lost one, and it is invisible — the only trace is a Cloudflare warning in a log nobody tails.
- 2026-09-02: **the Dashboard's FI countdown reads CLOSED months only, and that is the feature — not an optimisation.** Owner asked for "days before retirement" at the top of the Dashboard, to motivate reaching FI; it renders as **"X years, X months" with a progress bar** (the format was changed from years-and-days during the build — the payload still carries the exact `days`, only the display is coarse). The obvious build reads LIVE net worth and re-derives on every open; it is also useless, because the number then moves with the market — a red week adds a year and the thing reads as noise, which is the opposite of a spur. `api.fireEta` therefore takes **the last CLOSED month's `nw_snapshots` row** and **the three closed months before the live one**, and **anchors the projection to the first day of the current month, never to `today`**. The inputs only move at a month close, so the ETA DATE holds still for the whole month and the count falls on its own instead of jittering with the market. **Anchoring to `today` breaks both halves** — the date walks forward with you and the number never moves; `test.js` asserts the date is identical on the 2nd and the 27th and that the day count drops by 25 between them, which is the guard against someone "simplifying" the anchor away. **Three choices the owner made, not defaults:** target = 25x annual spend (the 4% rule, so a fat month pushes the date out twice — higher target AND lower savings, which is honest feedback); real return 5% (`fire_real_return`, a meta row so it retunes without a deploy); progress = closed-month net worth minus money LENT OUT, the same asymmetry the emergency runway makes. **Rejected: a regression over `nw_snapshots`** — data-driven and useless with about a year of history, since the slope is market noise and one bad month adds five years. **Rejected: the linear `gap / savings` answer** — simpler and wrong by years, because over a 15-year horizon the compounding IS most of the answer. **It ignores `month` and `months`** on purpose (own keys, own queries, not `ref`/`flowKeys`): browsing back to March must not re-date your retirement, and the chart window must not resize its inputs. `test-api.js` pins that. **Cost:** two more statements in `getDashboard`'s existing batch, no new round trip beyond the `metaGet`. **Nulls are real states:** no spend history returns null (no card); unreachable inputs return `days: null` — "never" is the honest word, not Infinity and not a date in 2400. The multiple stays a constant because 25x IS the 4% rule's definition, not a taste.
- 2026-09-01: **the back-merge merges when it cannot fast-forward — a hotfix no longer breaks it.** The `backmerge` job only ever ran `git push origin HEAD:develop`, on the belief written in its own comment: "develop is an ancestor of main in both cases, so this is a fast-forward". **That belief is true for a release and FALSE for a hotfix.** A hotfix branches off `main` and merges to `main`, so `develop` never sees it, and every commit already sitting on `develop` makes the two diverge. So the job did not fail on a rare race — it fails on **every hotfix taken while `develop` is not idle**, which is the normal state of `develop`. It broke on v2.9.1 with one merged PR (#39, the seed-category sync) waiting there, and the repair was three commands a person had to know to run. The fix keeps the fast-forward as the first attempt and, when it is refused, merges `main` into `develop` and pushes that — the same repair, unattended — with a 3-try loop because `develop` can move while the job runs. **Two limits kept on purpose:** ① a real content CONFLICT still stops the job, because only a person can decide which side wins, and ② nothing is ever forced (no rebase, no force-push, no history rewrite on `develop`) — the job may only add a merge commit. **Worth knowing:** the push carries `GITHUB_TOKEN`, so it starts no CI run (GitHub does not re-trigger workflows on a token push, and the old fast-forward push had the same property). Two green branches can still break each other when merged, so that merge commit's first real check is the CI on the next `develop` → `main` PR — which is the gate before `main` anyway. **How it was verified:** the script is extracted from `release.yml` itself and run against a throwaway origin in three shapes — clean fast-forward, diverged (today's failure), and a same-line conflict — asserting exit code AND whether `develop` ends up containing the release. The middle case is the one that used to fail. **This takes effect only when `main` carries the new workflow file**, because a push event runs the workflow as of the pushed commit; a hotfix landing before that release still needs the merge by hand.- 2026-09-01 (v2.9.1, hotfix): **the Quarterly pulse counts growth tickers only — an EF park is not investing.** IB01 is a treasury ETF, so it is share-priced, but its subtype is `EF` and the owner parks the emergency fund in it. The pulse derives its legs from account subtypes, and the broad test (`isSharesAcct`, which passes on `currency = 'Shares'` alone) took IB01 in: every EF top-up read as a quarter's investment, and the same peso was then reported twice — once as a pulse buy, once inside the Emergency-runway pool, which counts IB01 as cash-like on purpose. `db.js` gains **`isPulseAcct` = `isSharesAcct && isInvestedNetWorth`**, the third and narrowest of the account tests, and it is the exact complement of the runway's split: an account the runway counts as cash can never be a pulse buy. **The filter is in JS, not in the SQL.** The legs query also feeds the COST BASIS, and the Holdings card must keep showing IB01 with its cost and gain — narrowing `shareIds` would have deleted the position's basis to fix a bar chart. `symbol` is the ticker account's name on both arms of the UNION ALL, so one name set filters the rows the pulse may see and the basis still walks all of them. The guard test buys into an EF-subtype share account under the `Investment: Growth` CATEGORY on purpose: the exclusion is by account subtype, so it must hold when the category says otherwise. **Found while fixing it:** `test-api.js`'s runway test hard-coded "300 over 3 closed months" against a fixture with ABSOLUTE dates, so the real calendar rolled July and August into the averaging window on 2026-09-01 and `main` went red with nobody touching it. It now sums the same window it asserts on. A test that pins a figure derived from a moving window has a shelf life — derive the figure or make the dates relative, as `seed.sql` already does.
- 2026-08-31 (v2.9.0): **the data version is gone — a read carries an ETag over its own bytes.** The deferred plan was per-domain counters (`tx_version`/`ref_version`/`ledger_version`); it was designed, costed and then **rejected in favour of a hash**, and the reasoning matters more than the code. ① **Coverage.** The saving sets are nested, not competing: if no domain moved the payload is byte-identical, so the hash also matches — but the reverse fails constantly, and a counter cannot see SCOPE at all. Five of the seven cache keys are scope-keyed (`dashboard|month`, `budgets|month`, `tx|filters`, `tax|year`, `table|name|offset`), so a transaction entered today invalidated every past month, tax year and admin page the phone held. ② **Maintenance.** Per-domain converts a one-bit invariant into a matrix — 13 write handlers × the domains they touch, 9 reads × the domains they depend on — and **every cell fails silently** (a wrong domain shows a stale screen forever, with no error anywhere). It needed three source-text guards and a behavioural test to stay honest. The ETag has no invariant: the hash IS the truth, so it **deleted** the guard that already existed (`bumpStmt` in 13 batches, `version:` in 9 payloads, `getDataVersion`, `verKnown`/`VER_TTL`/`S.dataVersion`). ③ **Future work.** A new read handler, a new write handler, a new cron write and a new scope-keyed screen each cost the per-domain design a decision that can be got wrong, and the ETag design nothing. **The cron proved that failure mode was real, not theoretical:** `jobs.js` wrote `prices` and never bumped, so cached share values already went stale after the 06:00 job — per-domain would have asked someone to remember it, the ETag fixed it without being told. **The cost accepted:** a revalidation now runs the handler's full SQL and throws the bytes away on a match, where a version ping read one `meta` row. For one user on D1 that is noise, and it trades Cloudflare's compute (which is free here) for the owner's cell data and attention (which are not). **Two things a design review did not predict, both found by the tests:** ① **`getDashboard` moves on EVERY write anyway** — every month of it carries the live net-worth hero and the rolling cashflow, so January's bytes change when August does. The tag is telling the truth, so it is not a bug to fix by trimming the payload; the saving lives on Budgets, Tax and the admin pages instead, and there is a test pinning the behaviour so nobody "optimises" it later. ② **A read payload must never carry a clock** — one per-request value makes every tag unique and the whole saving disappears with nothing failing. `test-api.js` calls every read route twice and asserts the tag is stable (`getExportAll` exempt: `exportedAt` is deliberate and it is the backup puller's, not a screen's). **Also:** `getBudgets` now carries `recurring`, because two GETs meant two tags that could not 304 independently; `dropCache()` keeps the TAGS and clears only the freshness stamps, so a write leaves the screens it did not move able to answer 304; `Cache-Control: no-store` stays, since the SPA holds the tag in its own persisted cache — the browser's HTTP cache is never in the path, so there is one cache and it is the one that survives a reload and works offline. **`meta.data_version` is still in the table, unused**: dropping it in the same release would leave a `wrangler rollback` with a frozen counter and permanently stale screens (add in one release, remove in the next). **Verified in a real browser, not only in tests:** a relaunch 304s `getBootstrap` (the biggest payload), an August write moves August's budgets tag and leaves January's and `getRecurring`'s alone, and a stale tag comes back 200 with the full payload. Chrome logs a 304 to `fetch()` as `net::ERR_ABORTED` in the network panel — cosmetic, the promise resolves with `status: 304` and an empty body; confirmed directly before trusting it.
- 2026-08-31 (v2.8.3): **a reference code that keeps drawing 1020 is replaced, not replayed — and the v2.8.2 reasoning below is CORRECTED.** Probing the live Flex service settled what 1020 means: a **non-numeric token gets 1020** ("Invalid request or unable to validate request"), a **well-formed but wrong token gets 1015** ("Token is invalid"), and a deliberately malformed GetStatement URL (a doubled `?`) gets **1020**. So **1020 is IBKR's could-not-PARSE code, not a transient**, and the v2.8.2 justification — "SendRequest succeeded, so the token is valid, so 1020 must be transient" — does not hold: GetStatement is a DIFFERENT request, so SendRequest validating says nothing about it. What rescues the decision is a different fact: **our GetStatement shape is verified** — `q`+`t`+`v=3` against that same endpoint returns 1015 for a bad token, i.e. the request parses. A 1020 against a shape that parses therefore accuses the one part that is not static, **the reference code**, which is exactly what v2.8.2 replayed six times. Hence `FLEX_STALE_REF` (1017, 1020): the poll stops and `pricesJob` asks for **one** fresh code, then polls it briefly. **1017 breaks out immediately** rather than being polled — IBKR has already said that code is dead — so `flexPoll` returns a `{stale}` signal instead of throwing, and only a code needing a person throws. **Re-paced to IBKR's published limit** (one request per second, TEN per minute, per token — a limit easy to trip: four probe requests in a few seconds drew 1018): 6 tries at 6s plus a 2-try recovery = at most **10 calls in ~52s**, and a test asserts that ceiling. **`FLEX_PACE` is a parameter only so the tests can drive the two-code recovery without 50 real seconds of sleeps**; a contract guard holds `runCron` to `pricesJob(env)`, the real pace. The orchestration now has **real tests in `test-api.js`** against the live SQLite (stubbing only `fetch`), so the statement is parsed and the rows land for real: happy path, 1020→fresh code, 1017→fresh code with no wasted poll, a fatal code failing fast with its repair and no re-send, and two dead codes reporting the last reply. Three of them were proved to fail against v2.8.2. **Also learned:** `SendRequest` is on **ndcdyn** but the `<Url>` it returns is on **gdcdyn** — read it from the reply, never assume the host.
- 2026-08-31 (v2.8.2, reasoning superseded by the entry above): **the IBKR price poll retries every code IBKR calls transient, not just 1019.** The 06:00 job died with `Flex GetStatement error 1020: Invalid request or unable to validate request.` **The token was NOT the problem**, and that is the diagnostic worth keeping: `SendRequest` had already returned a ReferenceCode, which it cannot do on an expired (1012), invalid (1015), inactive (1011), IP-blocked (1013) or unknown-query (1014) token. So a failure at `GetStatement` clears the annual-token-renewal suspect by construction — read WHICH step threw before touching Client Portal. Root cause is ours, not IBKR's: the poll retried `ErrorCode 1019` and threw on everything else, but IBKR's table has TEN codes ending "Please try again shortly" (1001, 1004-1009, 1018, 1019, 1021) — any of them cost a whole night of prices. **1020 is retried too**: it is the catch-all "could not validate", and since SendRequest just passed on this token, every code that needs a person is ruled out and what remains clears by itself. `flexRetryable` is the split; a code outside it fails fast AND carries its repair in the Telegram message (`FLEX_FIX`), because "1012" alone does not tell the owner to make a new token. Two smaller fixes ride along: **the first `GetStatement` now waits 2s** — IBKR allows one request per second per token and `SendRequest` had just spent it, so the old zero-delay first poll both raced generation and sat on the limit — and **`xmlTag` tolerates an attribute and trims**, since a padded code matches nothing in the retry set and an attributed tag reads as absent, which hides the code from the failure message entirely. Guarded three ways: a unit test over the whole documented table, a `flexWhy` test for the padded/attributed tag, and a **contract guard** that `pricesJob` branches on `flexRetryable` and not on a literal — the silent failure is that comparing against one code looks right and passes every morning the statement is merely slow. Both guards were proved to fail before being trusted. **Not done:** re-running `SendRequest` for a fresh reference code on 1017, and making the sleeps injectable so a test could drive the poll end-to-end — a 7s test to prove what the guard already holds.
- 2026-08-30: **a name that names an account resolves to it — case is not part of the key.** "Foodpanda for Berry / 413.52 Maribank" was refused with `Unknown Account: Maribank` while `MariBank` sat in the table. Root cause: `refs()` indexes accounts and categories in a JS object keyed on the exact name, so every write path did an exact, case-sensitive lookup; the Gemini parser is told to copy a name character for character out of VALID ACCOUNTS but echoes the sender's spelling instead, and **the prompt taught it to** — rule 13's own example wrote "Maribank" twice. The fix is three layers, deliberately, because each catches what the one behind it cannot. ① **`db.js` gains `nameKey()`, a case-insensitive index beside the exact one, and `resolveAccount`/`resolveCategory`** — exact match first, case-insensitive behind it, and an ambiguous key (two names differing only by case) resolves to nothing rather than a coin flip. All five call sites in `api.js` use them, so the SPA, the bot, the Gmail courier and a raw API call all behave the same. The handlers bind the resolved row's **id**, so the stored row carries the canonical name whatever the caller typed. Two follow-ons: `createTransfer` compares the resolved **rows**, not the strings, or a self-transfer slips through under two spellings; `updateAccount` matches on `id` instead of `WHERE name = ?`. ② **The prompt's wrong example is corrected** and rule 3 now says do not echo the message's spelling. ③ **`telegram.js resolveAccountName`** widens further at the bot boundary only: exact → case-insensitive → letters-and-digits-only ("mari bank") → unique substring, the same test `/balance` has always made. Passes 2-4 take **one** candidate; two candidates pass the name through untouched so `createTransaction` throws its usual error instead of guessing, and the last two need three characters. The receipt is built from the resolved args, so the reply shows what was really written. **Not done:** `COLLATE NOCASE` on the name columns — `refs()` builds JS maps, so the collation is never consulted; it would need a table recreate and still not fix the lookup. Fuzzy resolution stays out of `api.js`: the API contract keeps exact-name semantics for everything but case.
- 2026-08-30: **`develop` deploys to a staging Worker.** `[env.staging]` in `wrangler.toml` → `financetracker-telegram-staging.<sub>.workers.dev`, its own D1 (`financetracker-staging`), deployed by `.github/workflows/staging.yml` on every push to `develop`. Purpose: check a change from a phone before the release PR is merged. **What the wrangler docs cost an hour to pin down:** most keys ARE inherited into a named environment — `main`, `compatibility_date`, `assets`, `workers_dev`, and **`triggers`** — while **bindings are not** (`d1_databases`, `kv_namespaces`, `vars`). Both directions bite. `assets` being inherited means the SPA needs no repetition (an earlier note here claimed otherwise — wrong). `triggers` being inherited is the trap: without `[env.staging.triggers] crons = []`, staging runs the 06:00 IBKR job and the net-worth snapshot against its own database on its own schedule. `name` is inherited WITH a suffix (`<name>-<env>`), so there is no `name` key and the never-rename rule is untouched. `kv_namespaces` is deliberately NOT repeated — FX_CACHE is optional, so staging just fetches every time. **Secrets are per-Worker and staging needs exactly ONE, `APP_PASS`** — no bot, no courier, no cron means `SECRET_TOKEN`/`TELEGRAM_*`/`GEMINI_API_KEY`/`INGEST_TOKEN`/`IBKR_*` are all dead weight there. **One real hole closed:** `telegram()` checks the header only `if (env.SECRET_TOKEN && ...)`, so an UNSET token leaves `/tg` an open write endpoint on a public hostname — `[env.staging.vars] SECRET_TOKEN = "staging-has-no-bot"` (a var, not a secret: a deliberately wrong value) makes every real Telegram POST fail closed. **A contract guard in `test.js`** holds the two silent failures: a staging `database_id` equal to production’s, and a missing crons override; both halves were proved to fail before being trusted. **Seeding is `workflow_dispatch` + `reseed` only**, never automatic — seeding on every push would wipe half-finished test state; `seed.sql` DELETEs first, so it is its own reset. **Staging never gets the real ledger:** a second copy doubles the blast radius of a leaked passphrase, and the point is behaviour, not balances. **Separate workflow file from `ci.yml`** because the two need opposite concurrency policies — CI cancels a superseded run, a deploy must never be cancelled between "migrations applied" and "Worker deployed".
- 2026-08-30: **the CI/CD path is now merge-and-forget, and local dev needs no credential.** Six changes, one theme — remove every step a person had to remember. ① **The release approval is gone.** The `production` GitHub environment carried a `required_reviewers` rule, so the workflow paused for a click that approved nothing the `develop` → `main` PR had not already approved. The rules are cleared through the API; the environment stays for its Deployments log, and `release.yml` says so, because a repo setting is invisible in the diff. ② **The back-merge is automatic.** After a merge into `main`, `develop` is an ancestor of `main`, so a `backmerge` job fast-forwards it with `git push origin HEAD:develop`. It runs on every push to `main`, released or not, and independently of the release job — `develop` must catch up even when a deploy fails. ③ **`ci.yml`, the gate that was missing:** `npm test` ran only in the local push hook (which no cloud-session PR and no browser merge pass through) and in the release job, on `main`, while deploying. Now every PR and every push to `develop` is tested. One job on purpose — `test-api.js` already applies the migrations AND `seed.sql` to a real SQLite database, so a `wrangler d1 migrations apply --local` step would download wrangler to re-prove that. ④ **`main` is protected** (PR + the `test` check, `enforce_admins` off). **`develop` is deliberately NOT** — a required check there rejects the backmerge job's push, whose commit carries no check of its own. The cost is that a `feature/*` → `develop` PR cannot use GitHub auto-merge; an agent watches `gh pr checks` and merges. ⑤ **`stamp-version.js`** hooks npm's `version` lifecycle, so `npm version patch` writes `index.html`'s `brand-ver` span. It was hand-edited, and forgetting it failed the release in CI, after the merge. ⑥ **Rollback is written down** (README): `wrangler rollback` returns the code in seconds, migrations never go back, so a destructive migration and the code that needs it must be two releases.
- 2026-08-30: **`wrangler dev` is not gated, and `worker/seed.sql` fills it.** Two things stopped an agent from checking a UI change: `/api` demanded the passphrase, which no agent may type, and a fresh local D1 was empty because the only filler is `npm run dev:pull` (the real ledger, denied to agents, and unavailable to a cloud session). `worker.js` `isLocalDev` skips the auth check when the request host is `localhost`/`127.0.0.1` — **not a flag in `.dev.vars`**, because a flag has to be planted in every checkout and could be set on the real Worker by accident; Cloudflare routes to a Worker BY HOSTNAME, so a production request cannot arrive that way. `worker/seed.sql` (`npm run dev:seed`) is invented data shaped like the real database: same six subtypes, the real category taxonomy, a USD contract salary into Wise, Wise→IBKR growth funding, broker buys and one sale, a card that is paid off, a refund as a negative expense, a Period override, an orphan ledger row for the ⚠ warning, and two `nw_snapshots` that agree with the seeded ledger so the dashboard bridge's residual is near zero. **Dates are RELATIVE** (`date('now','start of month', mo||' months', …)`, clamped to today), so the app always opens on a populated month and the file never goes stale. `test-api.js` applies it to the real migrated schema, so a schema change that breaks the seed fails `npm test`. It DELETEs before it inserts — re-runnable, and it replaces a `dev:pull` in the same local database.
- 2026-08-29: **the release moved from the owner's machine into GitHub Actions, so a cloud session can ship.** A cloud session cannot run the old `release.js`, for two independent reasons, and only one is a policy: ① the GitHub proxy in an Anthropic-hosted environment enforces **push protection** — `git push` reaches only the session's current working branch, so `git push origin main --tags` fails mechanically, and it fails AFTER `wrangler deploy` has already gone out; ② cloud environments have **no secrets store** — the docs state that environment variables are readable by anyone who uses the environment, so a `CLOUDFLARE_API_TOKEN` cannot live there. That token is not a build variable: Workers-deploy plus D1-write lets the holder deploy a Worker bound to `DB` and read every transaction, so it is a full-data credential. **The fix moves the trigger, not the credential.** `.github/workflows/release.yml` runs on a merge into `main`; the token lives in GitHub repo secrets; `release.js` keeps only the half that needs no credential — check the bump, run the tests, open the `develop` → `main` PR. Merging ships. A push to `main` whose version is already tagged does nothing, which is what keeps the hotfix flow (merge first, bump second) working. Side effect the owner wanted: a release no longer needs the laptop. **Not taken:** a self-hosted environment, which lifts both blockers but means running release infrastructure for a one-person project. `npm run push` (Apps Script) stays manual for good — clasp needs interactive OAuth, which no cloud session can do.
- 2026-08-29: **worktrees get a generated dev port, not the committed one.** `.claude/launch.json` pinned `wrangler dev` to 8123, so two worktrees fought over it. The file is now gitignored and written by `npm run bootstrap` (`.claude/launch.example.json` is the committed sample). The port is derived from the **checkout path**, not probed from what is listening: two idle worktrees bootstrapped the same day must still differ, and probing alone would hand both of them 8123. Bootstrap also runs `npm ci` and copies `worker/.dev.vars` in from the main checkout, the two other gitignored things a worktree does not inherit. It deliberately never runs `dev:pull` — that downloads real financial data.
- 2026-08-23 (**v2.0.1, hotfix**): **the daily-interest cron is deleted** — trigger, `interestJob`, `interestNetU` and their two tests. Owner: "I'm not using it." It was already retired for MariBank on 2026-07-26 (see below) because the bank's real credit never matched daily-balance×rate; nothing else used it. The `accounts.interest_frequency`/`interest_rate` columns, the Accounts-modal fields and the existing `interest-*` transactions are untouched — they are data, not the job. Back: `git show v2.0.0:worker/src/jobs.js`.
- 2026-08-23 (**v2.0.0, the platform swap — cut over and live; the one-shot migration kit is deleted, see `git show v2.3.1:D1_MIGRATION.md`**): **Google Sheets is replaced by Cloudflare D1, and the Telegram bot moved into the Worker.** This is the topology the 2026-08-22 entry below said was the only one that collects the win: the dominant cost was never the sheet read, it was the Apps Script invocation plus the mandatory `/exec` 302, so the only way to get it back is to delete GAS from the request path. Two things pinned the project to GAS in that analysis and both were solved rather than argued away — **`GmailApp`** by keeping a ~180-line courier in Apps Script that parses nothing and writes nothing (free OAuth to the owner's mailbox, no off-platform equivalent), and **`GOOGLEFINANCE`** by a nightly **IBKR Flex Web Service** cron into a `prices` table (all holdings are at IBKR, and price history is now an audit trail the Sheet never had). The third loss — the Sheet as an admin UI — is replaced by an **Admin screen**: a generic CRUD grid over a server-side table whitelist, no SQL console.
  - **The prime directive was that the `/api` JSON contract does not move.** Same `?action=` names, same argument names in the sheet's header casing (`Category`, `Amount (PHP)`), same `{status:'error'|'duplicate'}`, same `version` gate. Result: `worker/public/app.js` needed **no changes** outside the new Admin screen and the removal of the now-pointless `_v` stamp. `migrate/verify.js` captures a fixture per read route off live v1 and diffs v2 against it; **structural diffs are fatal, value diffs are for a human** (live FX and the GOOGLEFINANCE→IBKR price-source change legitimately move PHP figures on USD/Shares accounts — a PHP-only account that moves at all is a real bug).
  - **Money is integer micros** (native × 1e6), converted only at the API boundary. Exact sums, and the same column holds a fractional IBKR share quantity — a Shares row's amount always WAS a quantity in the sheet, so this is one representation, not two.
  - **`su_lock_()` + `cache_bumpVersion_()` collapsed into one thing:** every write puts its statements and `bumpStmt(env)` in the same `env.DB.batch()`. D1 batches are transactional, so there is no lock to take, no bump to forget, and no window where a reader sees the write but the old version. `test.js` asserts every `ROUTES_WRITE` handler mentions `bumpStmt` — the successor to the old "must end with cache_bumpVersion_" rule.
  - **Two SQL traps, both hit while writing the schema.** ① A generated column may only call **deterministic** functions and SQLite registers the date/time family as non-deterministic (they can read `'now'`), so `strftime` in the `month` expression fails at CREATE TABLE time — it is `substr`/`CAST` arithmetic instead, and `test.js` asserts the JS `monthOf()` and that expression agree. ② **`ROUND` before `CAST`** in `amount_php_u`: CAST alone truncates toward zero, which negative amounts expose immediately.
  - **The KV edge read cache (v1.9.2) is deleted, one release after shipping it.** It existed to hide Apps Script latency and D1 is the thing it was faking; the namespace is rebound as `FX_CACHE`. The client-side `_v` stamp went with it (it would now be a cache-buster on nothing) — the behavioural test that guarded it is replaced by one asserting it is *absent*. `cachedCall`/`S.cache`/the version gate stay untouched: they are the offline story, not a latency trick.
  - **The webhook URL does not change and `setWebhook` does not need re-running** — Telegram already delivered to the Worker's `/tg`; it just stops forwarding to GAS. `/tg` still answers 200 first and works in `waitUntil`, because Gemini is slower than Telegram's patience, and the update-id claim is still needed (it stops the retry *storm*; the idempotent row id only stops the second *row*). It moved from CacheService to a `seen_updates` table — own table, not `meta`, so the admin grid and the nightly export stay free of churn.
  - **The ⌕ Email button survives** because the courier ships the 1200-char quote with the payload into an `email_quotes` table; the Worker cannot call GmailApp, and the alternative was dropping the feature.
  - **Deliberately NOT done:** a SQL console in the admin screen (that is `wrangler d1 execute` from the owner's machine); `transactions` insert/update in the grid (real handlers own validation and FX stamping, and the grid must not route around them — delete-by-id stays, for surgery); writing IBKR position quantities (the ledger stays the source of truth for how many shares are held, IBKR only prices them); a `visibilitychange` revalidation (still unbuilt, still not worth the invocations).
  - **Rollback for the first week:** the Sheet is frozen, not deleted, and v1.9.2 is one `git checkout v1.9.2 && clasp push` + a `wrangler rollback` away. D1 Time Travel covers data mistakes for 7 days; the nightly Sheets dump covers forever.
- 2026-08-19: **Gmail ingest selects by the `Finance Tracker` label, not by a hard-coded sender list.** `GMAIL_QUERY_` is `in:inbox label:"Finance Tracker"` (`GMAIL_LABEL_`). Owner's reason: adding/removing a bank should be editing the Gmail filter that applies the label — a 10-second UI change — instead of a code edit + `clasp push` + redeploy, or a Script Property nobody remembers the syntax of. The senders themselves (`alerts@maribank.com.ph`, `mail.anthropic.com`, `wise.com`) survive only as a historical comment. Nothing else changed: the watermark, the idempotent `gm-<messageId>-<i>` ID, trash-on-log, the user lock and the `tg_parse_`/`tg_logItems_` reuse are untouched, so the receipt/⌕ Email/↻ Undo behaviour is identical. **Thread caveat, accepted:** Gmail search returns *threads* and the loop reads every message in them, so a label on one message pulls in its siblings — the same as the old `from:` query, and wanted (MariBank's alerts share one thread); the watermark still means one look per message. `test_gmailScope` guards the two invariants that must not drift: `in:inbox` (owner rule — the job never reads trash; `gmail_dumpSamples` stays `in:anywhere` for development) and no `from:` creeping back in. **Verified in production 2026-08-19:** the 5-min trigger picked up two labelled emails and logged both, no property changes needed. **Gotcha:** a `GMAIL_QUERY` Script Property would still win over the default (none is set) — check it first if labelled mail ever stops ingesting.
- 2026-08-10 (**shipped v1.4.0, deployment @60; `setupLedgerSchema` applied owner-side**): **Ledger rows reference a Salary transaction instead of duplicating it (issue #20).** Owner's real Ledger headers were `Date Received | Wise Amount | BSP Reference Rate | Total Income | Filed?`, so the migration adds only **two** columns — `Transaction ID` (FK → Transactions `ID`) and `8% Tax`. `Date Received`/`Wise Amount` become per-row `INDEX(MATCH())` lookups on tx `Date`/`Amount`; `Total Income` = `Wise Amount × BSP Reference Rate` (blank ⇒ 1); `8% Tax` = 8% of that. Typed cells left: `BSP Reference Rate`, `Filed?`. **Dropped from the first draft once the real headers landed:** `Description`, `Currency` and `Notes` columns — this ledger has never had them, the payout is always Wise/USD so `Currency` is constant, and `Ledger.gs` is header-generic so a hand-added `Notes` column would just work with zero code change. The deleted-tx warning moved onto `Date Received` (leftmost derived column) since there's no Description to carry it. **Per-row formulas, NOT ARRAYFORMULA** — ledger rows are appended/deleted one at a time and `api_deleteLedgerRow` does a real `deleteRow`, which would break a spill anchor. **`BSP Rate` is deliberately separate from the app's `ExchangeRate`**: BIR wants the central bank's reference rate on the date of receipt and the app has no feed for it, so it stays typed per row — that's the whole reason the manual column survives. **`Ledger.gs` needed zero changes**: its write guard is already `getFormula`-based, so the new derived cells are refused for free, and `api_appendLedgerRow` writes non-derived supplied columns, so "link a tx" is just `{Transaction ID: id}`. **Population = the issue's option (a), one-click, not auto-append on `api_createTransaction`** — no coupling of the tx write path to the tax sheet, and the owner stays in control of what gets filed; `api_getLedger` now also returns `unlinked` (Income: Salary tx no row points at) and the Tax screen renders them in a "Salary not in the ledger" card. Diff computed server-side, not client-side: one extra pass in an execution that already reads the sheet, and the join lives in one place. **Migration `setupLedgerSchema`** is additive (adds only missing columns, owner's extra columns untouched), backs the sheet up first, and **stamps formulas ONLY on rows it could link** — an unmatched legacy row keeps its typed figures rather than being blanked. Backfill matcher `mig_matchSalaryTx_` (pure, `test_matchSalaryTx`) links on date + |gross| and **returns null on ambiguity** (two same-day same-amount payslips) rather than guessing wrong into a tax filing. Deliberately NOT built: quarterly 2551Q/1701Q roll-ups (nothing consumes them yet), a per-quarter BSP reference table (rate is per-payslip), and a `deleteTransaction` guard for referenced rows — the Description formula falls back to `⚠ transaction deleted`, which is visible in the Sheet itself without coupling the two write paths.

## TODO / open questions
- **Owner-side, after v2.11.2 ships:** ① `npm run push` — the Gmail.gs fix reaches Apps Script no other way. ② Set the `gmail_ingest` trigger's failure notification to "Notify me immediately". ③ Optional: attach a standard GCP project so the Executions page's Cloud logs tab works. ④ To recover the three lost emails, delete the script property `GMAIL_LAST_TS` — the `gm-<messageId>-<i>` ids are idempotent, so nothing already recorded doubles.
- [x] **DONE 2026-09-02 (options ① then ②), see Decisions.** ~~A slow chat turn is reported but still lost.~~ ~~The ceiling moved to Telegram's undocumented patience.~~ Both halves shipped: `/tg` awaits the turn (v2.11.0), and the claim now carries the update so a turn that dies anyway is re-run by a 2-minute cron (v2.12.0). **Options ③ (Cloudflare Queues, Durable Objects) stay rejected** — both need the Workers PAID plan, and "Monthly cost: none" is a stated fact in README. Do not design toward them without the owner changing that constraint first.
- [x] **DONE 2026-08-30, see Decisions.** ~~A staging Worker for `develop` is designed but NOT built.~~ Shape: `[env.staging]` in `wrangler.toml` → `financetracker-staging`, its own D1, **no cron triggers**, no Telegram webhook, its own `APP_PASS`; a `staging.yml` deploys it on every push to `develop`, with its own `concurrency` group so two merges do not race one deploy. **Seed it from `worker/seed.sql`, never from prod** — a second copy of the real ledger doubles the blast radius of a leaked passphrase, and the point is checking behaviour, not balances. Non-obvious cost: `d1_databases`, `kv_namespaces`, `vars` and `assets` are NOT inherited by a named wrangler environment, so each is repeated under `[env.staging.*]`, and every secret needs `wrangler secret put --env staging`. **Alternative considered:** `wrangler versions upload` preview URLs — one command, no infrastructure, but the preview runs against the PRODUCTION D1, so it is safe for looking at a layout and unacceptable for anything that writes.
- **Two repo settings still need the owner's hands** (2026-08-30): the auto-mode classifier blocks a session from changing repository settings. `gh api --method PUT repos/:owner/:repo/branches/main/protection` (PR + the `test` check) and `gh api --method PATCH repos/:owner/:repo -F allow_auto_merge=true`. Do NOT enable `delete_branch_on_merge`: `develop` is unprotected, so GitHub would delete it when the release PR merges.
- [x] **DONE 2026-08-31 (v2.9.0), see Decisions — and the answer was NOT per-domain versions.** ~~Split `meta.data_version` into `tx_version`/`ref_version`/`ledger_version`.~~ An ETag over each read's own bytes replaced the counter outright.
- **IBKR Flex token expires within a year of creation.** Regenerating it is the one recurring maintenance task in this project; the README has a dated-task row for it. Symptom of expiry: the price cron Telegrams a failure and share values go stale.
- [x] **Not a bug:** Maya Savings 2026-02-08's two hand-entered `Income: Interest` rows (₱1.88 + ₱1.20) are **both correct** — owner confirmed 2026-07-29. Two same-day credits, not a double entry. Don't "fix" it in a future audit.
