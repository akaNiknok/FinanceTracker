{
  "name": "financetracker-gas",
  "version": "1.1.0",
  "description": "Google Apps Script backend for a personal finance tracker built on Google Sheets",
  "private": true,
  "scripts": {
    "login": "clasp login",
    "push": "clasp push",
    "watch": "clasp push --watch",
    "pull": "clasp pull",
    "open": "clasp open",
    "deploy": "clasp deploy",
    "release": "node release.js",
    "deployments": "clasp deployments",
    "logs": "clasp logs --watch"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
