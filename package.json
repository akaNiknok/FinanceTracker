{
  "name": "memento-mori",
  "version": "3.3.0",
  "description": "Personal finance tracker: Cloudflare Worker + D1, a PWA, and a Telegram bot",
  "private": true,
  "scripts": {
    "test": "node test.js && node --disable-warning=ExperimentalWarning test-api.js",
    "icons": "node icons.js",
    "dev": "cd worker && npx wrangler dev",
    "dev:pull": "node dev-pull.js",
    "dev:seed": "cd worker && npx wrangler d1 migrations apply memento-mori --local && npx wrangler d1 execute memento-mori --local --file=./seed.sql",
    "backfill:nw": "node migrate/backfill-nw.js",
    "deploy": "cd worker && npx wrangler deploy",
    "migrate": "cd worker && npx wrangler d1 migrations apply memento-mori --remote",
    "migrate:local": "cd worker && npx wrangler d1 migrations apply memento-mori --local",
    "tail": "cd worker && npx wrangler tail",
    "tail:staging": "cd worker && npx wrangler tail --env staging",
    "seed:staging": "cd worker && npx wrangler d1 execute memento-mori-staging --env staging --remote --file=./seed.sql",
    "login": "clasp login",
    "push": "clasp push",
    "pull": "clasp pull",
    "open": "clasp open",
    "version": "node stamp-version.js && git add worker/public/index.html",
    "release": "node release.js",
    "bootstrap": "node bootstrap.js"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
