# CLAUDE.md
Guidance for Claude Code (and humans) working in this repo. **Keep this file and MEMORY.md token-efficient: dense, minimal blank lines, no filler; lines may exceed 80 chars. Apply the same when editing them.**

## Session protocol (read first)
- **Start:** a **SessionStart hook** (`.claude/settings.json`) prints `HANDOFF.md` into context by itself; no print means no handoff. Then check MEMORY.md "TODO / open questions".
- **HANDOFF.md rule:** whenever a turn ends needing **human action** (owner-side migration/deploy/smoke-test, a decision, subagent output awaiting review), write/overwrite `HANDOFF.md`: ① what was done this session, ② exact pending human steps (commands verbatim), ③ how a fresh session resumes (files/memory entries to read). Purpose: owner can clear context and start a new session from it. Gitignored; superseded by the next handoff.
- **Load the skill before you edit the area:** `spa-frontend` for anything in `worker/public/`, `worker-backend` for `worker/worker.js`, `worker/src/*`, the migrations or the SQL. They hold the constraints that used to live in this file; do not re-derive them from the code.
- The big files are `worker/public/app.js`, `worker/src/api.js` and MEMORY.md — read targeted sections, not the whole file, when you know what you need. `MEMORY-ARCHIVE.md` is retired history; leave it closed unless the question is why something used to work that way.

## Communication style (how to talk to the owner)
Owner holds a **BS in Management Information Systems**. Bridge IT and strategy: frame a technical trade-off as system integration, scalability, data flow or value. Do not over-explain foundational tech and do not walk through low-level syntax.
**Write in Simplified Technical English (ASD-STE100 spirit, not the strict standard) — chat prose, commit messages, docs. README.md keeps the strict version (see its own rule).**
1. Use active voice. Write "The system sends the file", not "The file is sent by the system". 2. Put one idea in each sentence; split a sentence that has two. 3. Keep sentences short — no word count, but cut every word that adds nothing. 4. Use a maximum of three words in a noun cluster: "the log of failed jobs", not "the failed job log record". 5. Use one term for one thing — if it is a "data pipeline", it is never later a "data flow" or an "ingestion path". 6. Use one meaning for each word; do not use "run" for both execute and manage. 7. Start an instruction with the verb: "Open the panel", not "The panel must be opened". 8. Expand an acronym at first use only. 9. Use lists for steps and comparisons; use prose for reasoning.

## Project overview
**FinanceTracker** is a **personal finance tracker**. Since **v2.0.0 (2026-08-23) the backend is a Cloudflare Worker over Cloudflare D1** — one Worker serves the PWA, the JSON API, the Telegram bot and two cron jobs. Google Sheets is gone as the source of truth (frozen archive; the v1 backend is `git show v1.9.2:Transactions.gs` etc.).
- **Three ways in, one set of handlers:** the **PWA** (`worker/public/`), the **Telegram bot** (`worker/src/telegram.js`), and **Gmail ingest** (Apps Script courier → `?action=ingestEmail`). All route through `worker/src/api.js`.
- **What is left in Apps Script:** `Gmail.gs` (mail courier, 5-min trigger) and `Backup.gs` (nightly dump into a spreadsheet). **No Web App deployment, no `doGet`/`doPost`, no `deploymentId`, no `GAS_URL` secret.** `GmailApp` is the only reason GAS survives at all — free OAuth to the owner's mailbox with no off-platform equivalent.
- **Locale:** amounts in **PHP**; timezone `Asia/Manila`.
- **History:** v1 (GAS + Sheets, v1.0.0→v1.9.2) is the design record — `git show v1.9.2:CLAUDE.md` for the sheet-era lore, `git show v1.3.4:OVERHAUL_PLAN.md` for the original overhaul. The v2 cutover is done and its one-shot kit is deleted — `git show v2.3.1:D1_MIGRATION.md` for the execution plan, and the same for `Export.gs`, `migrate/import.js`, `migrate/verify.js`.

## Repository layout
**Two areas have their own skill — invoke it instead of asking here.** `worker-backend`: `worker/worker.js`, `worker/src/*.js`, `worker/migrations/*.sql`, `worker/wrangler.toml`, the D1 schema and the `/api` contract. `spa-frontend`: `worker/public/*` and `icons.js`. Everything else:
| File | Purpose |
| --- | --- |
| `Gmail.gs` | **The mail courier.** Same trigger, same user lock, same `GMAIL_QUERY_`/label/watermark/trash-on-success logic as v1 — but it parses nothing and writes nothing: `gmail_payload_` builds `{messageId,from,subject,date,hints,body,quote}` and POSTs it to `?action=ingestEmail` with the bearer token, and trashes the mail only when the response says every item landed (`logged === total`), **per message, never per thread** (MariBank's alerts share one thread). `quote` travels with the payload because the Worker cannot call GmailApp — that's what the receipt's ⌕ Email button reads back out of `email_quotes`. Holds `cfg_`/`worker_url_`/`worker_token_`, shared with `Backup.gs` through the GAS flat namespace. Keeps `gmail_dumpSamples`. |
| `Backup.gs` | Nightly `GET ?action=getExportAll` → rewrites one tab per table in a dedicated "FinanceTracker Backup" spreadsheet (plain `setValues`, no formulas — it's a dump). Creates the book on first run and remembers its id in `BACKUP_SHEET_ID`. `backup_install()` adds the trigger. Failure → email to the effective user (there's no Telegram token in GAS any more). |
| `Tests.gs` | What is left to test in GAS: the courier's pure helpers. `PURE_TESTS` is driven by `test.js`. |
| `migrate/backfill-nw.js` | **One-shot** (`npm run backfill:nw > backfill-nw.sql`): reconstructs pre-cron monthly net worth into `nw_snapshots`. Loads the `dev:pull` dump into an in-memory `node:sqlite`, reuses the app's own `deltas`/`shapeAccounts`/`netWorthTotals` so a backfilled figure matches a live one, and values each month with historical prices + USD/PHP from **Yahoo Finance's v8 chart endpoint** (keyless, browser UA; covers the LSE UCITS ETFs via `.L` and USD/PHP via `USDPHP=X`, returns the quote currency). `fetchDailySeries` is the swap point if Yahoo ever refuses; **IBKR can't** (Flex has no date param, no PHP FX) and **Stooq's keyless CSV is JS-PoW-walled**. `SYMBOL_MAP` maps IBKR ticker→Yahoo where they differ (US names identical). A series that fails is non-fatal: months needing it are **skipped and reported**, never half-valued (`ON CONFLICT DO NOTHING`, so real snapshots are never clobbered). `--self-test` checks the valuation + as-of lookup with no DB/network. |
| `test.js` | Half of `npm test`. Three layers: the `.gs` leftovers through a `vm`, `worker/src/*` as plain ESM, and the **contract guards** (route naming, every write bumps the version, both create paths keep their idempotency clause, button glyphs, the SPA parses and no longer stamps `_v`). No account of any kind. |
| `test-api.js` | The other half: **the handlers against a real in-memory SQLite** (`node:sqlite`, the same engine D1 runs) through a small D1 shim. This is what exercises the SQL — it is how the `""`-as-identifier bug and both generated-column traps were caught, and it covers the things only a database can answer (duplicate detection, the ledger view's joins, end-to-end balances). Needs Node 22+; older Node skips the file rather than failing. **`batch()` is not transactional in the shim** — the tests do not exercise rollback and faking it would be pretending. |
| `release.js` | `npm run release`, run on **develop**. It does NOT release: it checks the version bump against `origin/main`, runs `npm test`, and opens the `develop` → `main` PR. Merging that PR is what ships. |
| `bootstrap.js` | `npm run bootstrap`. Makes a fresh clone or a new git worktree runnable: `npm ci`, copies `worker/.dev.vars` in from the main checkout, and generates a gitignored `.claude/launch.json` whose dev port is derived from the checkout path — so two worktrees never fight over 8123. Never runs `dev:pull` (real financial data). |
| `.github/workflows/staging.yml` | **`develop`’s deploy target.** Every push to `develop` applies the migrations to `financetracker-staging` and deploys `financetracker-telegram-staging` — so a change can be checked from a phone before the release PR is merged. A **separate file from `ci.yml` on purpose**: the two need opposite concurrency policies, because CI cancels a superseded run and a deploy must never be cancelled between "migrations applied" and "Worker deployed". It does not run the tests (`ci.yml` does, on the same push) and does not `npm ci` (only wrangler runs). **Seeding is `npm run seed:staging` or `workflow_dispatch` + `reseed`, never automatic** — seeding on every push would wipe whatever you were half way through testing. **`workflow_dispatch` needs the workflow file on the DEFAULT branch**, so the Run-workflow button does not exist until a release carries `staging.yml` into `main`; `npm run seed:staging` is the path that always works. |
| `.github/workflows/ci.yml` | **The gate before develop.** `npm test` on every pull request and every push to `develop`. It exists because `npm test` otherwise ran only in the local push hook — which a cloud session's PR and a browser merge both miss — and in the release job, by which point the code is already on `main`. Deliberately ONE job: `test-api.js` already applies every migration and `worker/seed.sql` to a real SQLite database, so a `wrangler d1 migrations apply --local` step would download wrangler to re-prove that. `test` is the required check on `main`. |
| `stamp-version.js` | npm's `version` lifecycle script: `npm version patch|minor` copies the new number into `index.html`'s `brand-ver` span and stages it. The span used to be hand-edited, and forgetting it failed the release in CI after the merge. `test.js` still asserts the two agree — that guard now catches a hand-edit, not a forgotten one. |
| `.github/workflows/release.yml` | **The only path to live.** Runs on a merge into `main` whose `package.json` version is not yet tagged: `npm test` → D1 migrations → `wrangler deploy` → tag → GitHub Release. Needs the repo secrets `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID`. |
| `.claude/settings.json` | Project settings + hooks (committed; the ONLY settings file a cloud session reads). Holds `attribution` (no commit/PR trailer), `permissions.deny` on the real-financial-data files, **SessionStart → prints `HANDOFF.md`**, and **PreToolUse(Bash) → `.claude/hooks/guard-push.js`**, which blocks a push to `main` and runs `npm test` before any other push. Put a rule here, not in this file, when a shell one-liner can enforce it; Claude Code asks to approve the hook once per machine. `guard-push.test.js` is its self-check. |
| `HANDOFF.md` | (when present, gitignored) previous session's handoff — read first. |
| `MEMORY.md` | Durable notes, decisions, secrets-locations, TODOs (read it too). Retired v1/Sheets-era decisions and closed TODOs live in `MEMORY-ARCHIVE.md` — history, not context; do not load it unless the question is about why something used to work that way. |
| `README.md` | **The human's doc — this file is the AI's.** Two halves: a **portfolio half** (tagline, What it does, mermaid diagram, Engineering decisions, Facts, Known limits) and a **Maintenance half** (hosting, secrets, triggers, IBKR token renewal, fault isolation, rebuild). Rules when editing: **① public repo — never a URL, id, token or passphrase value; name the secret, not the value. ② Simplified Technical English (ASD-STE100)** — one idea per sentence, active voice, simple tenses, no contractions/possessives/gerunds-as-verbs, ≤20 words per instruction, same word for the same thing; the opposite of this file's dense style, don't carry that across. **③ concise** — add a row to a table, not a new prose section. **④** Facts figures must be re-counted, and every Engineering decision is a real incident from MEMORY.md — never invent one. |

## Git workflow (gitflow + releases)
- **`main`** = released/live code only. **`develop`** = integration branch; day-to-day commits and merges land here.
- **Features:** `feature/<name>` off `develop`, merge back to `develop`. **Create it with `git switch -c feature/<name> --no-track origin/develop`.** `git checkout -b <name> origin/develop` sets the UPSTREAM to `origin/develop`, so a bare `git push` from that branch lands straight on `develop` and there is then no branch to open a PR from (this happened on 2026-08-30). With `--no-track` the branch has no upstream and a bare push refuses until you name one.
- **Release: a merge into `main` is the ONLY way code reaches live.** `.github/workflows/release.yml` runs on that merge — `npm test`, D1 migrations, `wrangler deploy`, tag `v<package.json version>`, GitHub Release, in that order (new code must never meet an old schema). A push to `main` whose version is already tagged does nothing, so the hotfix flow reaches live on the bump commit. **No credential is needed locally any more**; the Cloudflare token lives in GitHub repo secrets.
- **Preparing a release:** bump on `develop` (`npm version patch|minor --no-git-tag-version`, commit). **`stamp-version.js` now writes `index.html`'s `brand-ver` span for you** and stages it — do not hand-edit it. Then `npm run release`, which **does not release** — it checks the bump, runs the tests and opens the `develop` → `main` PR. Merging that PR ships.
- **Hotfix:** `hotfix/<name>` off `main` → merge to `main`, bump patch. The workflow releases on the bump commit.
- **The back-merge is automatic (2026-08-30).** Every push to `main` runs the `backmerge` job, which fast-forwards `develop` to `main`. `develop` is always an ancestor of `main` after a merge, so it needs no merge commit. A diverged `develop` fails that job instead of forcing anything — merge by hand and the next push is clean again. **The release is also unattended:** the `production` GitHub environment has NO protection rules, so the merged PR is the only approval.
- **Tags/Releases invariant:** every tag is `vX.Y.Z`, on `main`, created only by the Release workflow, with a matching GitHub Release. Never hand-create tags or Releases.
- **`develop` deploys to staging, `main` deploys to live.** `[env.staging]` in `wrangler.toml` → `financetracker-telegram-staging`, its own D1, **no cron, no bot, no Gmail courier**, one secret (`APP_PASS`). Wrangler inherits most keys into a named environment but **not the bindings** — and `triggers` IS inherited, so the empty `crons = []` override is what stops staging running the 06:00 IBKR job against its own database. `test.js` guards both traps: every `[[d1_databases]]` needs an `[[env.staging.d1_databases]]` copy whose `database_id` DIFFERS, and the crons override must be present. Staging holds `seed.sql` data only — **never a copy of the real ledger**.
- **`main` is protected (2026-08-30):** it takes a pull request only, and the `test` check must be green. `enforce_admins` is off, so the owner keeps an escape hatch. `develop` is deliberately NOT protected — a required check there would reject the `backmerge` job's fast-forward push, whose commit carries no check of its own.
- **An agent works on a branch, not on `develop`:** `feature/<name>` (or `fix/<name>`) off `develop`, then a PR into `develop`. CI runs on the PR. Wait for it (`gh pr checks --watch`) and merge; do not merge red. Push `develop` freely by hand; never push directly to `main`. The push hook enforces the second half, CI the first.
- **Undoing a release:** `npx wrangler rollback` puts the last good Worker version back in seconds. **Migrations do not roll back** — so an old Worker must still run against the new schema. Never ship a destructive migration (a dropped or renamed column) in the same release as the code that needs it; add in one release, remove in the next. README has the procedure.
- **Worktrees** (`git worktree add .claude/worktrees/<name> -b <branch>`) let a hotfix run beside unfinished `develop` work with nothing stashed. **Run `npm run bootstrap` inside a new one before anything else** — `node_modules`, `worker/.dev.vars` and the dev port are all gitignored, so a worktree inherits none of them. Never run `npm run release` from a worktree, and remove one when its branch merges. `.claude/worktrees/` is gitignored.

## Conventions & gotchas
- **Never commit credentials** or any URL/id that acts as one. `.clasp.json` (`scriptId` only) is safe; `worker/.dev.vars` and `.clasprc.json` are gitignored.
- **Every "today" goes through `manilaToday()`.** A defaulted transaction date and the BIR quarter window are the two places a UTC slip silently corrupts data.
- **Month keys are `yyyy-MMM` with exactly that capitalisation, everywhere.**
- **The Growth segment rides the FUNDING transfer** (Wise→IBKR, category `Investment: Growth`, budget Monthly $200 = "did I park this month's $200"); broker buy legs (IBKR→ticker/IB01) and EF parking are `Transfer: Internal` — tagging both legs double-counts budget actuals. **There is deliberately NO Stability category or budget row** (removed 2026-08-26): EF accrues as unspent residue, which a monthly meter can't track — the Emergency-runway card is the only EF measure. The convention lives in the category DESCRIPTIONS (which the Gemini prompt inlines, so the bot follows it); `getInvestments`' pulse derives buys from share-priced accounts, not categories, so it needs no discipline. Runway = the `isInvestedNetWorth` liquid side minus receivables minus credit.
- **A UI-only change is not live until the Worker redeploys** — `worker/public/` ships with the Worker, so it rides the same release as backend code; there is no separate asset upload. Nobody runs `wrangler deploy` by hand any more, the merge into `main` does it. A GAS change is the exception: `npm run push` from the owner's machine and nothing else (no deployment exists, and clasp needs interactive OAuth, so no session can do it).
- **Accounting conventions (v2.5.0/v2.6.0, from the accounting-soundness review).** ① **A refund is a NEGATIVE-amount Expense row in the ORIGINAL category** — never `Income: Other`, which overstated both sides. ② **Reports are sign-aware**: budgets, dashboard spend/flow and the runway's average spend sum `amount_php_u` signed, never `ABS()`, so a refund nets its category down; Telegram's `querySummary` keeps `ABS` on purpose ("how much moved"). ③ **A cross-currency transfer that lands in PHP stamps the IMPLIED rate** (`ToAmount/Amount`), not the live rate — the live rate hid the conversion spread; an explicit `ExchangeRate` still wins. ④ **The month close is yesterday's month**: the cron stamps `monthOf(manilaYesterday())`, so the run on the 1st closes the previous month with its last day in it. ⑤ **A negative receivable is a liability** — it reports under liabilities and shortens the emergency runway; a positive one is neither (money lent is not reachable). ⑥ **`liabilities` is NEGATIVE everywhere** — API, `nw_snapshots`, `netWorthTotals`; only the SPA hero absolutes it. ⑦ **`amount_u` is never zero** — rejected on every write path AND a DB CHECK since `0003_tx_checks.sql` — but it may be negative. ⑧ **The `transactions` invariants live in the SCHEMA (v2.6.0):** `amount_u != 0`, `(to_account_id IS NULL) = (to_amount_u IS NULL)`, `fx_rate IS NULL OR fx_rate > 0`, and a real month/day in `date`. A `wrangler d1 execute` escape hatch now fails on a bad shape too — that is the point. The migration recreates the table (SQLite cannot add a CHECK in place); it **copies the rows FIRST**, so a row that breaks a CHECK aborts it with the live schema untouched. ⑨ **A SELL is a transfer OUT of the ticker account** (Amount = share quantity, ToAmount = the money); on a share-priced source `amount_u` is a QUANTITY, so **every peso aggregate that can see a Transfer carries `NOT_SHARES_SRC`** (db.js) — budgets today; the bot's `querySummary` does the same check in JS. Dashboard spend/flow are Income/Expense-only and need none. ⑩ **Cost basis is average-cost** (v2.7.0): a sale removes cost in proportion to the shares leaving, so the average entry price never moves on a sale. `costPhp` is historical (the rate stamped on each buy leg), `investedNative` is the house-money figure (cash in minus proceeds out) and goes negative; a position funded from two currencies reports no native figure at all.
- **Local dev needs no passphrase and no real data (2026-08-30).** `/api` skips the auth check when the request host is `localhost`/`127.0.0.1` (`isLocalDev` in `worker.js`) — Cloudflare cannot route a production request to that host, so it is inert once deployed. `npm run dev:seed` fills the LOCAL D1 from `worker/seed.sql`, invented data modelled on the real database's shape; it REPLACES whatever is there, a `dev:pull` included. So an agent can now open the SPA and read every screen. `npm run dev:pull` is still the real ledger and still denied to agents.
- **A new pure helper gets a test in `test.js`;** anything touching SQL or a handler gets one in `test-api.js`; a new invariant whose failure is silent gets a contract guard. (The push hook already runs `npm test` for you.)
- Keep **MEMORY.md** current: decisions, trigger setup, secret locations.
- **Keep README.md current whenever a change touches the human's world** — a new/renamed secret or `meta` key, a new trigger or schedule, a new hosted service, a changed deploy/migration step, a new manual setup, a new failure mode worth a troubleshooting row, or a stack change. Pure code/refactor/UI-behaviour changes need nothing. Same commit as the change. **Public repo: names of secrets, never values or URLs.**
