# clasp / Apps Script auth credentials — NEVER commit these
.clasprc.json
~/.clasprc.json

# Node
node_modules/
npm-debug.log*

# OS / editor
.DS_Store
*.log

# Session handoff notes (per-session artifact, not history)
HANDOFF.md

# Personal financial-system docs — real PII, lives in the private enchiridion repo
docs/

# Copy of the n8n container's /home/node/.n8n — holds encryptionKey + credentials
n8n-data/

# Wrangler local state/cache
worker/.wrangler/

# Local `wrangler dev` secrets (APP_PASS, the bot/Gemini/IBKR tokens)
worker/.dev.vars

# Prod D1 dump pulled by `npm run dev:pull` — real financial data, never committed
worker/.dev-data.sql
# Reconstructed net-worth SQL from `npm run backfill:nw` — real financial data
backfill-nw.sql
worker/.dev-backfill.sql

# v2 migration artefacts — real financial data, never committed
migrate/fixtures/
seed.sql
financetracker-export-*.json
